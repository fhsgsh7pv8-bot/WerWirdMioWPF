<Page x:Class="WerWirdMioWPF.View.EndPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="EndPage">

    <Page.Resources>
        <Storyboard x:Key="GtaMissionPassedAnim">
            <DoubleAnimation Storyboard.TargetName="RespectImage"
                             Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0" 
                             Duration="0:0:2.0" />

            <DoubleAnimation Storyboard.TargetName="ImageScale"
                             Storyboard.TargetProperty="ScaleX"
                             From="2.0" To="1.0" 
                             Duration="0:0:1.5">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <DoubleAnimation Storyboard.TargetName="ImageScale"
                             Storyboard.TargetProperty="ScaleY"
                             From="2.0" To="1.0" 
                             Duration="0:0:1.5">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Page.Resources>

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard Storyboard="{StaticResource GtaMissionPassedAnim}" />
        </EventTrigger>
    </Page.Triggers>

    <Grid>
        <MediaElement x:Name="BackgroundVideo"
                      LoadedBehavior="Manual" 
                      UnloadedBehavior="Stop"
                      Stretch="UniformToFill"
                      IsMuted="True" 
                      MediaEnded="BackgroundVideo_MediaEnded" />

        <Image x:Name="RespectImage" 
               Source="pack://application:,,,/images/respect.png" 
               Stretch="None" 
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Opacity="0"
               RenderTransformOrigin="0.5,0.5"
               Panel.ZIndex="1">
            <Image.Effect>
                <DropShadowEffect Color="Gold" 
                                  BlurRadius="25" 
                                  ShadowDepth="0" 
                                  Opacity="1.0" 
                                  RenderingBias="Performance"/>
            </Image.Effect>

            <Image.RenderTransform>
                <ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1" />
            </Image.RenderTransform>
        </Image>
    </Grid>
</Page>